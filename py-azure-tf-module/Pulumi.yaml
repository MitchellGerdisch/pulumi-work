name: azure-tf-module-py
description: A minimal Python Pulumi program with the classic Azure provider
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: azure-classic-python
