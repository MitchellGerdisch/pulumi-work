name: deploy-sched-test-ts
runtime: nodejs
description: A minimal TypeScript Pulumi program that configures it's own deployment settings and schedules
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
