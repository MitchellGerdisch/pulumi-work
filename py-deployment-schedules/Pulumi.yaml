name: deploy-sched-test-py
runtime:
  name: python
  options:
    virtualenv: venv
description: Python program that sets up it's own deployment settings and schedules
packages:
  pcloudsettings: https://github.com/mitchellgerdisch/component-pcloudsettings
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
